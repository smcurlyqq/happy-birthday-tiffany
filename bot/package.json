{
  "name": "seoul-loop-bot",
  "version": "1.0.0",
  "private": true,
  "description": "LINE bot that files links dropped in the group into the Seoul Loop Notion boards",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
